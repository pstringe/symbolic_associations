export interface EnerergyTransferInputs {
    source: string;
    initial: string;
    final: string;
    constraint: string;
}
  
export interface CausalInputs {
    trigger: string;
    outcome: string;
}